
include conf.d/*.conf


<match {local,aws}.**>
  type influxdb
  dbname test
  flush_interval 10s # for testing.
  host <%= @influxdb_host %> # (localhost by default)
  port 8086 # (8086 by default)
</match>
